class QBit {
  constructor() {
    this.x = x;
    this.y = y;
    this.size = 20;
    this.fill = 0;
  }

  spawnQBit() {
    // spawn QBits over time
  }

  // check corruption level here or in the main script?
  checkCorruption() {
    // check for max q-bits threshold value and trigger fail condition
  }

  dispay() {
    // display a rotating black box in a random position.
  }
}